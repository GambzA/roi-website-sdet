{
  "name": "roi-gamba-portfolio",
  "version": "2.0.0",
  "description": "Personal portfolio of Roi Mark Gamba — SDET and Software Engineer",
  "private": true,
  "scripts": {
    "build": "tailwindcss -i ./src/input.css -o ./dist/styles.css --minify",
    "watch": "tailwindcss -i ./src/input.css -o ./dist/styles.css --watch"
  },
  "license": "UNLICENSED",
  "devDependencies": {
    "@tailwindcss/cli": "^4.3.3",
    "tailwindcss": "^4.3.3"
  }
}
